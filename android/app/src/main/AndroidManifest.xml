<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Necesario para descargar los tiles del mapa. Flutter solo lo añade
         automáticamente en debug/profile, así que en release hay que declararlo
         a mano o el mapa de fondo sale en blanco. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- Permisos de ubicación: necesarios para leer el GPS y desvelar la niebla
         al moverse. FINE = precisión alta (GPS), COARSE = aproximada (red). -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <!-- Ubicación en segundo plano (app cerrada / pantalla bloqueada). -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>
    <!-- Servicio en primer plano: mantiene vivo el seguimiento con una
         notificación permanente. FOREGROUND_SERVICE_LOCATION es obligatorio
         desde Android 14 (API 34) para servicios de tipo ubicación. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- Notificaciones: avisar al descubrir un POI con la app minimizada
         (Android 13 / API 33+ exige pedir este permiso en tiempo de ejecución). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- usesCleartextTraffic=false: bloquea cualquier conexión http:// sin
         cifrar (todos nuestros proveedores van por https). En API 28+ ya es el
         valor por defecto; esto lo garantiza también en dispositivos viejos. -->
    <application
        android:label="Fog of War"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- Vuelta del login con Google: el navegador redirige a
                 fogofwar://login-callback y supabase_flutter captura la sesión
                 (kAuthRedirectUri en lib/cloud/cloud_config.dart). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="fogofwar" android:host="login-callback"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
